apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: movie-ingress
spec:
  rules:
  - host: movie.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx
            port:
              number: 8080
              